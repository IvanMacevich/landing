import {createTheme} from "@mui/material";

export const theme = createTheme({
    palette: {
        mode: 'light',
        primary: {main:'#fff'},
        background: {
            default: '#fff',
        },
        text: {
            primary: '#123'
        }
    },
})